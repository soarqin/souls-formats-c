# 2026-05-11
- Kept the test strict on structure only: size, parse success, entry count, and at least one non-NULL queried item text.
- Added `souls_formats_test_fmg_e2e_er` as a custom alias target so the requested build command works without renaming the existing executable target.
